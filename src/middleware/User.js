// // import React from 'react';
// // import { Link, Navigate } from "react-router-dom";
// // import { getAuthUser } from "../helper/Storage";

// // const User = () => {
// //   const auth = getAuthUser();
// //   return <>{auth && auth.role === 0 ? null : <Link to={"/"} />}</>;
// // };

// // export default User;

// const User = () => {
//   const auth = getAuthUser();
//   return <>{auth && auth.role === 0 ? <Link to={"/"} /> : null}</>;
// };
